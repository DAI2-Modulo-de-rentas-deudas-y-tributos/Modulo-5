# Docker Compose: PostgreSQL y publicación de puertos
POSTGRES_DB=rentas
POSTGRES_USER=rentas
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_PORT=5432
SERVER_PORT=8080

# Ejecución directa de Spring Boot desde la terminal o el IDE
DB_URL=jdbc:postgresql://localhost:5432/rentas
DB_USER=rentas
DB_PASSWORD=CHANGE_ME
OUTBOX_DELAY_MS=5000

# Único adaptador disponible actualmente; no configura un broker real
BROKER_ADAPTER=local-log

# Sólo DEMO/DEV: se usa para crear los cinco usuarios iniciales si dev-mode=true
RENTAS_SECURITY_DEV_MODE=false
RENTAS_DEMO_BOOTSTRAP_PASSWORD=CHANGE_ME
