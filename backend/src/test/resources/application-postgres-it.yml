spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
rentas:
  security.dev-mode: true
  # Evita que el publicador programado contamine los contadores SQL de las
  # pruebas de performance; el claim concurrente se valida explícitamente.
  outbox-delay-ms: 600000
