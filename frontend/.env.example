# URL pública del backend de Rentas. Vacía = la app usa el dataset de demostración.
VITE_API_BASE_URL=http://localhost:8080

# Fuerza el dataset local aunque VITE_API_BASE_URL esté configurada.
VITE_USE_MOCKS=true
